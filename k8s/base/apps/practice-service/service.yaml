apiVersion: v1
kind: Service
metadata:
  name: practice-service
  labels:
    app.kubernetes.io/name: ai-interview-tutor
    app.kubernetes.io/component: practice-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: ai-interview-tutor
    app.kubernetes.io/component: practice-service

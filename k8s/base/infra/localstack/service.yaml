apiVersion: v1
kind: Service
metadata:
  name: localstack
  labels:
    app.kubernetes.io/name: ai-interview-tutor
    app.kubernetes.io/component: localstack
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4566
      targetPort: 4566
  selector:
    app.kubernetes.io/name: ai-interview-tutor
    app.kubernetes.io/component: localstack
